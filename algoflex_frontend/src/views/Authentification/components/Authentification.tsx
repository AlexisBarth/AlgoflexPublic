import { Login, Signup } from '@components'

const Authentification = () => {

    return (
        <div>
            <Login/>
            <Signup/>
        </div>
    );
}

export default Authentification;