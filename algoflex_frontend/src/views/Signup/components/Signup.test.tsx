import Signup from './Signup'
import {render} from '@testing-library/react'
import '@testing-library/jest-dom'

test.todo("Make Signup tests");

test('load component Signup', async () => {
    render(<Signup/>)
});